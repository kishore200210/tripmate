# ─────────────────────────────────────────────────────────
# TripMate v2.0 — Official Backend Dependencies
# All packages are from the approved technology stack.
# ─────────────────────────────────────────────────────────

# ── Core Framework ────────────────────────────────────────
fastapi[standard]>=0.115.0          # Async REST framework + built-in uvicorn/httpx
pydantic>=2.8.0                     # Request/Response validation and DTOs
pydantic-settings>=2.4.0            # Typed environment variable management

# ── Database ──────────────────────────────────────────────
sqlalchemy[asyncio]>=2.0.36         # ORM with async support
psycopg[binary,pool]>=3.2.0        # Modern async PostgreSQL driver (v3)
pgvector>=0.3.2                     # pgvector Python adapter for SQLAlchemy
alembic>=1.13.3                     # Schema migration tool for SQLAlchemy

# ── Authentication & Security ─────────────────────────────
python-jose[cryptography]>=3.3.0    # JWT token creation and verification
pwdlib[argon2]>=0.2.1               # Argon2 password hashing (PHC winner)

# ── AI / LLM Orchestration ────────────────────────────────
openai>=1.40.0                      # OpenAI SDK for chat, embeddings
groq>=0.9.0                         # Groq SDK for fast inference
langchain>=0.3.0                    # LLM orchestration and chains
langchain-openai>=0.2.0             # LangChain OpenAI integration
langchain-groq>=0.2.0               # LangChain Groq integration
langgraph>=0.2.0                    # Stateful AI agent graphs
tiktoken>=0.7.0                     # Token counting for context window management

# ── Machine Learning ──────────────────────────────────────
numpy>=2.0.0                        # Numerical computing
scikit-learn>=1.5.0                 # ML models and pipelines
pandas>=2.2.0                       # Data manipulation
joblib>=1.4.2                       # Model serialization

# ── Computer Vision ───────────────────────────────────────
ultralytics>=8.3.0                  # YOLO11 for landmark/object detection
opencv-python-headless>=4.10.0      # Image processing (headless, no GUI deps)
Pillow>=10.4.0                      # Image manipulation and I/O

# ── Background Jobs ───────────────────────────────────────
celery[redis]>=5.4.0                # Distributed task queue
redis>=5.0.8                        # Redis client for Celery broker + caching

# ── PDF Generation ────────────────────────────────────────
weasyprint>=62.3                    # HTML/CSS to PDF for itinerary exports
jinja2>=3.1.4                       # Templating for HTML/PDF generation

# ── HTTP Client ───────────────────────────────────────────
httpx>=0.27.0                       # Async HTTP client for external API calls

# ── Monitoring & Logging ──────────────────────────────────
sentry-sdk[fastapi]>=2.13.0         # Error tracking and performance monitoring
logfire[fastapi]>=0.50.0            # Structured observability and logging
prometheus-fastapi-instrumentator>=7.0.0 # Prometheus metrics for FastAPI

# ── Utilities ─────────────────────────────────────────────
python-multipart>=0.0.9             # File upload support (multipart/form-data)
python-dotenv>=1.0.1                # Local .env file loading

# ── Testing ───────────────────────────────────────────────
pytest>=8.3.0                       # Test runner
pytest-asyncio>=0.23.8              # Async test support
pypdf>=5.0.0
