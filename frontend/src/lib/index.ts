/**
 * app/lib/index.ts
 *
 * Barrel export for the lib/ utility module.
 *
 * What belongs here:
 *   - API client (axios instance)
 *   - Utility functions (formatDate, formatCurrency)
 *   - Constants (API_BASE_URL)
 *   - Type guards
 *
 * What does NOT belong here:
 *   - React components (→ components/)
 *   - Hooks (→ hooks/)
 *   - Global state (→ store/)
 */

// Exports will be added as utilities are implemented.
export {};
